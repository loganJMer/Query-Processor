Session 1: 12pm to 3pm, September 18
Began working on the project. Created all the necessary files and started by simply implementing most of the relational algebra operator functionalities including projection, times, union, etc. Did not implement selection and join as those need parser for condition. Will just implement join as times + select though. Also implemented method to convert a relation in provided string format into list of tuples. Used AI for occasional small troubleshooting and for the relation print method for debugging. Also began working on tokenizer.

Session 2: 5pm - 7:30pm, September 18
Finished tokenizer, adding functionality for all possibilities as for all that I'm currently aware of. Swapped relations to store table name at index 0, which allows creation of a new table name after operation. This is necessary when having many nested functions like multiple cartesian products to have accurate attribute names. Also implemented required tests for the tokenizer, will probably add some custom ones for edges cases later.
**AI issue**: Used it to automatically translate the required tests into my list format of [query, expected]. Two issues arose. Firstly, it tried using different token names like LEFTBRACKET, RIGHTCURLY instead of the ones I had and provided it. Secondly, it was unable to recognize what should be a tokenizer error vs parser error. For example, it set the expected output for cases 16 and 17 to SYNTAX ERROR, when they should just be tokenized and the tokenizer will parse them correctly. *note: I know tests beyond 7.1 are not to test tokenizer but I figured it was good to add and it heplped me remember some cases I'd forgotten like commas*
Planning to begin work on grammar


Session 3: 2pm - 3:30pm, September 19
Read some info on EBNF to start. Then began working on my grammar document. First did the definitions of relations and strings to get a hang of the formatting without having to worry about expression order and stuff. Then, I began working on the actually expression grammar. It was tough to figure out how to both solve the left-recursion issue as well as how to add associativity and precedence. However, upon asking AI, although it gave me incorrect grammar and I described below, it did introduce me to the concept of working backwards. I did the comparison operators first, starting with or which is lowest precedence and then moving towards not. I then did the same thing for binary/unary expressions, starting with the lower binary operations, then doing join, followed by unary. After working out some typo or oversight kinks with review and AI, I managed to get the grammar to a state I liked. I then quickly answered the questions 5.2, 5.3, and 5.4. Grammar may have to be tweaked after beginnig to actually write the parser but looking good for now.
**AI issue**: When trying to use AI to help confirm if there were issues with my grammar declarations for section 5.1, it continually tried to make me changed my definitions so that the base definition of an expr was exclusively a binary operator, essentially making it so every query would have to have at least one binary operator


Session 4: 4:30pm - 8pm, September 20
Began working on the parser, starting with the parse_expr() function. Took a while to wrap my head around how to properly structure this to work like my grammar. After a while, it clicked that I was trying to use recursion wrong, and that the recursion is not used for chaining together the various binary operators at the top level like I have, but it is used to recall various methods when inside parentheses. After this realization, I structred my main method around a while loop, chaining through join-expr's and binary ops. I tested this using a fake parse_join_expr method that assumes it is a single relation identifier and it worked! I repeated the logic for join and primary expression operations, then went to and condition parsing. I used AI to help me figure out how to structure the NOT parsing, since it is right associative instead of left like my previous, and it was good! Worked up most of the parser logic top include unary, primary expressions, and made some test with AI. I'm noticing some issues but I'm done for today so that is a tomorrow problem

**AI issue**: For many of these methods, AI would often try to add many redundant error checks for situations either impossible due to a current nested condition in an if/while or due to how data is passed from a lower function. WHile caution doesn't hurt in some situations, it would do these to extreme degrees for problems that would be essentially impossible without major breakages in the code, at which point the code would've already failed


Session 5: 1am - 3am, September 21
Couldn't sleep, so went back to fix the parser issues. Basically two issues. Firstly for parse_join_expr I added the wrong amount of tokens consumed for rightPrimaryExpr so I had to fix that. Also I had to change my None evaluation to the format result = parse(); if not result: return; expr, tokens = result since returning None when it expected two vars was also messing up. Then I went and did the evaluator, mostly simple especially since I already implemented it earlier for conditions, just a big match case. Ended up having to make some slight changes around like select not parsing the condition itself just evaluating, making all operators check if evaluation worked in case there is a relation that doesn't exist, rename had to also change the internal table name to aid with times. Otherwise used AI to generate some tests and find the above edge case issues and fix them. Will probably do some more rigourous testing tomorrow and dcide how I want to implement the actual user table entry cause I don't really get how we're supposed to do that right now but whatever. Basically, the main functionality is done so let's go

Session 6: 3:10am - 3:45am, Septermber 21
Still couldn't sleep, so I added the main loop functionality. Now function can be run regularly, will allow for multi line input for relation definition, and then will accept queries as well. Can also be run in command line with --tree "QUERY" which will print out parse tree instead of evaluating. Also fixed edge case where tokenizer wouldn't recognize trailing binary op "A union" as a binary op since it didn't have a space after, however that's necessary so parser will give correct error message